{
  "name": "meerkats-app-mockup",
  "version": "1.0.0",
  "private": true,
  "description": "Hi-fidelity interactive mockup of the Meerkats.ai growth intelligence product",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/styles.css --minify",
    "build:site": "npm run build && rm -rf public && mkdir -p public/dist && cp index.html public/ && cp dist/styles.css public/dist/",
    "dev": "tailwindcss -i ./src/input.css -o ./dist/styles.css --watch",
    "serve": "python3 serve.py"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
